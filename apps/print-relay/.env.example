# URL WebSocket de votre backend (celle donnée par Render, en remplaçant https par wss)
BACKEND_WS_URL=wss://le-tandoor-backend.onrender.com/ws

# Doit être exactement la même valeur que PRINT_RELAY_TOKEN configuré sur le backend (Render)
PRINT_RELAY_TOKEN=
